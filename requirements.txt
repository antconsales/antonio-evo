# Antonio Evo - Dependencies
#
# CORE (required)
requests>=2.28.0

# API SERVER (for WebSocket + REST)
fastapi>=0.100.0
uvicorn>=0.22.0

# CONFIGURATION (for .env support)
python-dotenv>=1.0.0

# IMAGE/ML FEATURES (optional - install from requirements-optional.txt)
# See requirements-optional.txt for: torch, transformers, diffusers, etc.

# OPTIONAL - External APIs (install only if using fallback)
# anthropic>=0.18.0
# openai>=1.0.0

# OPTIONAL - Development
# pytest>=7.0.0
# black>=23.0.0
# mypy>=1.0.0
